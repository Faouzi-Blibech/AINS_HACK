# Cassette — Agent Execution Tracer & Deterministic Replay Engine
# Python dependencies (prototype). Pinned loosely for the hackathon.

# --- Core / API layer ---
fastapi
uvicorn[standard]
pydantic

# --- Agent + LLM ---
# AI layer calls Groq's OpenAI-compatible API directly over httpx (see ai_agents/llm.py)
python-dotenv        # loads .env so GROQ_API_KEY is picked up automatically
langgraph            # optional: graph-based agent under test
langchain-core

# --- Interception layer ---
mitmproxy            # HTTP/TLS proxy for LLM + REST tool calls
httpx                # lightweight proxy / client wrapper
mcp                  # MCP client/server for the MCP proxy

# --- Trace + blob store ---
# SQLite ships with Python's stdlib (sqlite3). No extra dep for the prototype.

# --- Eval ---
pytest
