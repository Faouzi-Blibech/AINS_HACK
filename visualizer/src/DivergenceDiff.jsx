// DivergenceDiff — side-by-side original vs forked/replayed trace.
//
// Highlights the step where the two trajectories separate and diffs the
// downstream steps. Used after a record-over fork or a counterfactual variant.
//
// Skeleton only.

export default function DivergenceDiff(/* { baseRunId, forkRunId } */) {
  // TODO: align the two traces by step, mark the fork point, diff downstream.
  return null;
}
