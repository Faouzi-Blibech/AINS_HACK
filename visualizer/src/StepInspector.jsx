// StepInspector — exact detail for a selected step.
//
// Shows the LLM prompt, context window, tool-call arguments, and response,
// reconstructed from blob-store references. Surfaces the step's confidence
// score and side_effecting flag.
//
// Skeleton only.

export default function StepInspector(/* { runId, stepId } */) {
  // TODO: resolve blob refs, render prompt/response/args, show confidence.
  return null;
}
